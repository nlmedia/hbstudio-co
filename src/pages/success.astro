---
import Base from '../layouts/Base.astro';
import { useTranslations, getLocaleFromPath } from '../i18n';

const locale = getLocaleFromPath(Astro.url.pathname);
const t = useTranslations(locale);
---
<Base title={t.success.meta.title} description={t.success.meta.description}>
  <section class="mx-auto max-w-2xl px-5 py-32 text-center">
    <div class="mx-auto grid h-16 w-16 place-items-center rounded-2xl grad-accent text-3xl text-white">✓</div>
    <h1 class="mt-6 font-display text-4xl font-bold">{t.success.heading}</h1>
    <p class="mt-3 text-muted">{t.success.body}</p>
    <div class="mt-8 flex flex-wrap justify-center gap-4">
      <a href="/templates" class="rounded-full bg-ink px-6 py-3 font-medium text-paper transition hover:bg-accent">{t.success.browseMore}</a>
      <a href="/contact" class="rounded-full border border-line-strong px-6 py-3 font-medium transition hover:bg-surface-2">{t.success.needHelp}</a>
    </div>
    <p class="mt-6 text-xs text-muted" id="ref"></p>
  </section>
  <script define:vars={{ i18nOrderReference: t.success.orderReference }}>
    const id = new URLSearchParams(location.search).get('session_id');
    if (id) document.getElementById('ref').textContent = i18nOrderReference + id.slice(-12);
  </script>
</Base>
